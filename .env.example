# .env.example
# このファイルをコピーして .env を作成してください。
# cp .env.example .env
#
# 本ファイルはリポジトリに含まれます（サンプルとして）。
# 実際の .env ファイルは .gitignore で除外されています。

# ----------------------------------------
# Discord Bot
# ----------------------------------------
# 管理者ユーザー ID (DM 送信先)
ADMIN_USER_ID=000000000000000000
# Bot を登録するギルド (サーバー) の ID
GUILD_ID=000000000000000000

# ----------------------------------------
# Database (MariaDB)
# ----------------------------------------
DB_HOST=127.0.0.1
DB_NAME=bot_db
DB_USER=root
DB_PASS=

# ----------------------------------------
# Rust Bridge (database_bridge)
# ----------------------------------------
BRIDGE_HOST=127.0.0.1
BRIDGE_PORT=8088

# ----------------------------------------
# 権限マスミュート機能
# カンマ区切りでチャンネル名を列挙する。
# Python (config.py) と Rust Bridge (database_bridge) 両方から参照される。
# ----------------------------------------
# [通知抑制のみ] 送信は許可するが @mention・Webhook は禁止するチャンネル
MUTE_ONLY_CHANNEL_NAMES=配信コメント

# [閲覧専用] 送信・@mention・Webhook をすべて禁止するチャンネル
READ_ONLY_CHANNEL_NAMES=参加ログ
